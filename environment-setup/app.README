This readme will walk you through the process to get the application up and running

#######################

Open a terminal to the vagrant and then log in as root, and run the following commands.

cd /web_root
composer install
php artisan migrate
php artisan seed

After that logout back to the host machine, and run `npm install` in the code directory.